%p= "Vous avez trouvé  #{@result_listing.count} utilisateur(s) qui ont parrainés le plus de monde"

%table.ui.single.line.table
  %thead
    %tr
      %th Fullname
      %th sponsorship total
  %tbody
    - @result_listing.each do |l|
      %tr
        %td
          = l[:user]
        %td
          = l[:count]
